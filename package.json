{
  "scripts": {
    "build": "npx eleventy",
    "serve": "npx eleventy --serve",
    "release": "git subtree push --prefix dist origin master"
  },
  "devDependencies": {
    "@11ty/eleventy": "git+https://github.com/mechpen/eleventy.git",
    "markdown-it-anchor": "^5.2.4",
    "markdown-it-katex": "^2.0.3",
    "markdown-it-prism": "^2.0.3",
    "sass": "^1.22.12"
  }
}
